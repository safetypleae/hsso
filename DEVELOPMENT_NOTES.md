# HSSO 개발 기록

마지막 정리일: 2026-08-31

## 1. 프로젝트 개요

HSSO는 안전·보건관리자를 위한 웹서비스의 첫 번째 MVP로, 현재 핵심 기능은 브라우저 기반 MSDS 경고표지 제작이다.

- 정적 HTML, CSS, JavaScript로 구성한다.
- React, Next.js, Vue 등의 프레임워크와 npm 패키지는 사용하지 않는다.
- 백엔드, 로그인, 데이터베이스는 아직 없다.
- 실제 배포 및 브라우저 테스트 기준 주소는 `https://hsso.pages.dev`이다.
- `file://` 환경은 PDF.js 모듈 제약 때문에 기능 검증 기준으로 사용하지 않는다.

## 2. 현재 구현 상태

### 사용자 흐름

- PDF 파일 선택 및 드래그 앤 드롭
- PDF 파일 형식 확인, 파일명·크기 표시, 선택 취소
- 경고표지 크기 선택 및 직접 크기 입력
- PDF.js를 이용한 브라우저 내부 PDF 텍스트 추출
- 전체 페이지 및 페이지별 텍스트 추출 상태 확인
- 제1항·제2항·제3항 범위 탐색
- 제품명, 공급자, 연락처, 신호어, H-code, P-code 추출 시도
- 추출 결과 직접 수정 및 미리보기 실시간 반영
- 추출 원문과 개발용 분석 정보 확인
- PDF 오류, 텍스트 부족, 스캔형 가능성 안내

PDF는 외부 분석 서버로 전송하지 않고 사용자 브라우저 안에서 처리한다.

### 경고표지

- 제품명, 그림문자, 신호어, 유해·위험문구, 예방조치문구, 공급자 정보 영역으로 구성된다.
- 예방조치문구는 예방·대응·저장·폐기로 구분한다.
- 각 예방조치 분류의 전체 분석 데이터는 유지하고, 미리보기에는 최대 7개까지 표시한다.
- GHS01~GHS09 그림문자를 사용자가 직접 선택하거나 해제할 수 있다.
- 자동 그림문자는 MSDS 제2항의 실제 그림문자 영역에서 명확한 근거가 있을 때만 확정하도록 설계되어 있다.
- H-code, 유해성 분류명 또는 일반 위험 문장만으로 그림문자를 추정하지 않는다.
- 근거가 부족하면 `확인 필요` 상태를 유지한다.

### 아직 구현하지 않은 기능

- 서버 기반 분석 및 데이터 저장
- 로그인과 사용자 계정
- OCR 기반 스캔 PDF 분석
- 모든 제조사 양식을 포괄하는 고정밀 범용 파서
- 신뢰할 수 있는 GHS 이미지·벡터 자동 식별 완성
- 최종 PDF 다운로드 및 법정 규격 검증

## 3. 실제 MSDS 테스트 데이터셋 조사

`test-msds/`에 실제 MSDS PDF 16개가 있으며, 해시 비교 결과 동일 문서 두 쌍을 제외하면 고유 PDF는 14종이다.

- 16개 모두 텍스트 레이어에서 문자를 추출할 수 있었다.
- 이번 데이터셋에서 완전한 스캔형 PDF는 발견되지 않았다.
- 대부분의 문서에서 제1항·제2항·제3항 위치를 확인할 수 있었다.
- 한 문서는 텍스트 레이어의 단어가 여러 번 중복되어 현재 섹션 탐지가 실패했다.
- 여러 문서에서 제2항이 다음 페이지까지 이어졌다.
- 위험성 정보가 있는 다수 문서의 제2항 페이지에서 이미지 객체가 발견됐다.
- 이미지 객체가 실제 GHS 그림문자인지는 별도의 영역 추출과 이미지 비교 없이 확정하지 않았다.

`test-msds/`는 `.gitignore`에 포함되어 있으며 테스트 PDF는 Git에 추적되지 않는다.

## 4. 현재 파서에서 확인된 문제

### 섹션 및 텍스트 구조

- 섹션 제목의 가운데점, 마침표, 공백, `제1항` 형식 차이에 대응 범위가 부족하다.
- 표 형태 PDF에서는 추출 순서가 화면의 읽기 순서와 다를 수 있다.
- 페이지가 바뀌는 제2항에 반복 머리말·꼬리말이 삽입된다.
- 같은 단어가 여러 번 겹친 텍스트 레이어를 정상화하지 못한다.

### 필드 라벨

- 글자 사이가 벌어진 제품명 라벨을 안정적으로 인식하지 못한다.
- 한글 열거 기호, 원문자 번호 또는 구두점이 앞에 붙은 라벨을 놓친다.
- 공급자·유통자·제조자·담당부서 라벨의 변형을 충분히 구분하지 못한다.
- 일반 전화번호와 긴급전화번호의 역할 구분이 불완전하다.
- 숫자와 하이픈 사이에 공백이 있는 전화번호를 놓칠 수 있다.

### H/P-code 및 문장 결합

- 코드 문자와 숫자 사이 또는 숫자 내부에 공백이 들어가면 코드를 놓칠 수 있다.
- 복합 P-code의 `+` 주변에 공백이 있으면 불안정하다.
- 여러 줄로 나뉜 문구에 페이지 머리말이 끼면 문장에 불필요한 내용이 포함될 수 있다.
- 일부 표에서는 예방·대응·저장·폐기 제목과 P문구의 텍스트 추출 순서가 뒤섞여 단순 순차 분류가 틀릴 수 있다.
- 코드 자체는 찾았어도 대응 문구가 잘리거나 다음 영역과 합쳐지는 부분 성공 사례가 있다.

### GHS 그림문자

- 이미지 객체 수만으로는 로고, 표 장식 및 실제 GHS 그림문자를 구분할 수 없다.
- 하나의 이미지 객체 안에 여러 그림문자가 합쳐져 있을 수 있다.
- 벡터 명령은 표 선과 일반 도형도 포함하므로 명령 수만으로 그림문자를 판단할 수 없다.
- 현재 자동 판정은 근거가 부족한 경우 선택하지 않는 안전 우선 방식이다.

## 5. 문서 구조 유형

- 유형 A: 일반 텍스트형 MSDS
- 유형 B: 표 셀 때문에 텍스트 순서가 뒤섞이는 MSDS
- 유형 C: 제2항이 여러 페이지로 이어지는 MSDS
- 유형 D: 페이지 경계에서 반복 머리말·꼬리말이 문장에 삽입되는 MSDS
- 유형 E: H/P-code와 전화번호 내부에 공백이 삽입되는 MSDS
- 유형 F: 동일 단어가 반복되는 비정상 텍스트 레이어형 MSDS
- 유형 G: 그림문자가 개별 또는 복합 이미지 객체로 저장된 것으로 보이는 MSDS
- 유형 H: 표와 도형이 다수의 벡터 명령으로 저장된 MSDS

## 6. 다음 작업: 범용 파서 개선 방향

### 1단계: 추출 데이터 모델 개선

- 페이지 번호, 원문, x/y 좌표, 글꼴 크기와 텍스트 항목 순서를 함께 보존한다.
- 단순 문자열 결합 전에 같은 기준선의 텍스트를 행으로 재구성한다.
- 열 간격과 좌표를 이용해 표 셀 및 다단 구조를 구분한다.

### 2단계: 안전한 정규화

- 섹션명과 알려진 라벨에 한해서 글자 사이 공백과 열거 기호를 정규화한다.
- 본문 전체의 공백은 무조건 제거하지 않는다.
- H/P-code는 제한된 패턴 안에서만 공백을 제거하고 원문도 함께 보존한다.
- 동일 위치 또는 연속 구간의 반복 토큰을 좌표 기반으로 제거한다.

### 3단계: 섹션 탐지기

- 항 번호, 제목 핵심어, 문자열 유사도를 함께 평가한다.
- 제2항 시작부터 제3항 직전까지를 페이지 경계와 무관하게 하나의 범위로 유지한다.
- 탐지 결과에 페이지, 위치, 원문과 신뢰도를 기록한다.

### 4단계: 반복 머리말·꼬리말 제거

- 여러 페이지의 상단과 하단에서 반복되는 텍스트 및 좌표를 찾는다.
- 문서번호, 개정 정보, 페이지 번호 등을 제거하되 그 뒤의 H/P문구는 보존한다.

### 5단계: 필드 추출

- 제품명, 공급자, 제조자, 유통자, 일반 전화, 긴급전화 후보를 역할별로 수집한다.
- 라벨과 값의 좌우·상하 위치 관계를 이용한다.
- 자동 선택한 값과 선택하지 않은 후보를 모두 디버그 정보로 남긴다.

### 6단계: H/P문장 파서

- 정규화한 코드에서 시작해 다음 코드, 분류 제목 또는 섹션 제목 직전까지 문장을 결합한다.
- 페이지가 바뀌어도 같은 문장을 이어 붙인다.
- 예방·대응·저장·폐기는 텍스트 순서뿐 아니라 제목과 표 셀의 좌표 관계를 이용한다.
- 추론으로 문구를 생성하지 않고 PDF에 실제 존재하는 코드와 문구만 사용한다.

### 7단계: GHS 객체 분석

- 제2항의 `그림문자` 라벨과 해당 표 셀의 경계 영역을 찾는다.
- 그 영역 안의 이미지 XObject와 벡터 그래픽만 후보로 수집한다.
- 복합 이미지는 개별 마름모 영역으로 분할한 뒤 표준 GHS 자산과 비교한다.
- 이미지 또는 벡터를 확정할 근거가 부족하면 자동 선택하지 않는다.

### 8단계: 회귀 테스트

- 파일명이 아닌 PDF 내부 내용만 입력으로 사용한다.
- 현재 14종의 고유 문서를 회귀 테스트 데이터셋으로 사용한다.
- 섹션, 필드, H/P-code, 분류, 그림문자를 항목별로 성공·부분 성공·실패·확인 불가로 기록한다.
- 파서 변경 전후 결과를 비교하고 기존에 성공하던 문서가 퇴행하지 않는지 확인한다.

## 7. 안전 원칙

- 파일명이나 제조사명에 의존하는 제품별 하드코딩을 추가하지 않는다.
- PDF에 없는 제품 정보, H/P문구, 신호어 또는 GHS 그림문자를 생성하지 않는다.
- 자동 분석값에는 원문 위치와 근거를 연결한다.
- 불명확한 값은 빈 값 또는 `확인 필요`로 유지하고 사용자가 수정할 수 있게 한다.
- 실제 배포 검증은 Cloudflare Pages 주소에서 수행한다.

## 8. 다음 작업 시작점

다음 개발 작업은 디자인 변경보다 좌표 기반 텍스트 재구성, 코드 정규화, 반복 머리말 제거를 먼저 구현하는 것이 적절하다. 이후 14종 고유 PDF에 대한 회귀 테스트를 수행하고, 마지막 단계에서 제2항 그림문자 셀의 이미지·벡터 식별을 별도 모듈로 확장한다.
